{
  "name": "kane-wang-report",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "seed": "node scripts/extract-seed.mjs",
    "check": "node --check server.js && node --check lib/db.js && node --check lib/content.js && node --check admin/app.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
